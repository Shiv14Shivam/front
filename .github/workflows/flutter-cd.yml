name: Flutter CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'  # Matches SDK ^3.9.2+ compatible
        
    - name: Get dependencies
      run: flutter pub get
      
    - name: Verify formatting
      run: dart format --output=none --set-exit-if-changed .
      
    - name: Analyze code
      run: flutter analyze
      
    - name: Run tests
      run: flutter test

  build-deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push'  # Deploy only on push to main
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'
        
    - name: Get dependencies
      run: flutter pub get
      
    - name: Build web
      run: flutter build web --release
      
    - name: Deploy to Vercel
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.ORG_ID }}
        vercel-project-id: ${{ secrets.PROJECT_ID }}
        working-directory: ./
        vercel-args: '--prod'

# Optional: Firebase deploy (uncomment after SA setup)
#  firebase-deploy:
#    runs-on: ubuntu-latest
#    needs: test
#    if: github.event_name == 'push'
#    
#    steps:
#    - uses: actions/checkout@v4
#    
#    - uses: subosito/flutter-action@v2
#      with:
#        flutter-version: '3.24.0'
#        
#    - run: flutter pub get
#    - run: flutter build web --release
#    
#    - uses: FirebaseExtended/action-hosting-deploy@v0
#      with:
#        repoToken: '${{ secrets.GITHUB_TOKEN }}'
#        firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SANDHERE_WEB }}'
#        channelId: live
#        projectId: sandhere-web

