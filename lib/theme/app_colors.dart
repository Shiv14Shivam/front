import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFFF3F6FF);
  static const Color surface = Colors.white;

  // Primary Brand
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryLight = Color(0xFF60A5FA);
  static const Color primaryDark = Color(0xFF1E40AF);

  // Vendor
  static const Color vendor = Color(0xFF16A34A);

  // Accent
  static const Color accent = Color(0xFFBDDcff);

  // Text
  static const Color titleText = Color(0xFF111827);
  static const Color bodyText = Color(0xFF6B7280);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Borders / soft UI
  static const Color border = Color(0xFFE5E7EB);
}
